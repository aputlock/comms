module Comms.POP3
  ( handleConn
  ) where

import           Comms.Common.Types
import           Network
import           System.IO

--handleConn :: Handle -> Config -> t -> IO Handle
handleConn :: t2 -> t1 -> t -> a
handleConn handle config channel = undefined
